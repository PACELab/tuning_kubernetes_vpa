apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-15m
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-deployment-15m
  template:
    metadata:
      labels:
        app: nginx-deployment-15m
    spec:
      containers:
      - image: nginx
        name: nginx-container-15m
        resources:
          limits:
            cpu: 50m
          requests:
            cpu: 25m
