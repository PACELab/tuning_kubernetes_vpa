apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-deployment-test
  template:
    metadata:
      labels:
        app: nginx-deployment-test
    spec:
      containers:
      - image: nginx
        name: nginx-container-test
