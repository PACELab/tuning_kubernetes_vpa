apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-deployment-default
  template:
    metadata:
      labels:
        app: nginx-deployment-default
    spec:
      containers:
      - image: nginx
        name: nginx-container-default
        resources:
          limits:
            cpu: 50m
          requests:
            cpu: 25m
